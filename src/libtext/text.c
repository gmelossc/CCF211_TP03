#include "text.h"

void Inicializa(Text *texto){
    Tcel =(Apontador)malloc(sizeof(Tcel));
    texto->initial=NULL;
    texto->tamanho=0;
}
void InsereTexto(Text *texto,char texto){
    Tcel *aux;
    aux->
}